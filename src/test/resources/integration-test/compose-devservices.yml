services:
  db:
    image: postgres:17
    healthcheck:
      test: pg_isready -U user -d test
      interval: 5s
      timeout: 3s
      retries: 3
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: test