apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atm-layer-reporting-service.fullname" . }}-test-connection"
  labels:
    {{- include "atm-layer-reporting-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atm-layer-reporting-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never